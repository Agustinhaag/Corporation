<title>Create user</title>
<link rel="stylesheet" href="Css/create.css" />
<main>
  <section class="container-gral">
    <form action="/user" method="post" enctype="multipart/form-data">
      <div class="container-title"><h2>Crear perfil</h2></div>
      <div class="container-form">
        <div class="container-input">
          <div class="container-info">
            <label for="name">Nombre:</label>
            <input
              type="text"
              placeholder="Nombre"
              class="form-control <%= errors && errors.find(error => error.path === 'name') ? 'is-invalid' : '' %>"
              name="name"
              id="name"
              value="<%=values.name%>"
            />
            <% if (typeof errors !== "undefined") { %> <% const nameError =
            errors.find(error => error.path === 'name'); %>
            <div class="invalid-feedback">
              <%= nameError ? nameError.msg : '' %>
            </div>
            <% } %>
          </div>
          <div class="container-info">
            <label for="edad">Edad:</label>
            <input
              type="number"
              placeholder="Edad"
              name="edad"
              class="form-control <%= errors && errors.find(error => error.path === 'edad') ? 'is-invalid' : '' %>"
              id="edad"
              value="<%=values.edad%>"
            />
            <% if (typeof errors !== "undefined") { %> <% const edadError =
            errors.find(error => error.path === 'edad'); %>
            <div class="invalid-feedback mb-1">
              <%= edadError ? edadError.msg : '' %>
            </div>
            <% } %>
          </div>
        </div>
        <div class="container-data">
          <label for="descripcion">Carta de presentación:</label>
          <textarea
            placeholder="Descripción"
            name="descripcion"
            id="descripcion"
            cols="20"
            rows="5"
          >
<%=values.descripcion%></textarea
          >
        </div>
        <div class="container-input">
          <div class="container-info">
            <label for="email">Email:</label>
            <input
              type="email"
              placeholder="Email"
              class="form-control <%= errors && errors.find(error => error.path === 'email') ? 'is-invalid' : '' %>"
              name="email"
              id="email"
              value="<%=values.email%>"
            />
            <% if (typeof errors !== "undefined") { %> <% const emailError =
            errors.find(error => error.path === 'email'); %>
            <div class="invalid-feedback">
              <%= emailError ? emailError.msg : '' %>
            </div>
            <% } %>
          </div>
          <div class="container-info">
            <label for="nacimiento">Fecha de nacimiento:</label>
            <input
              type="date"
              class="form-control <%= errors && errors.find(error => error.path === 'nacimiento') ? 'is-invalid' : '' %>"
              placeholder="Fecha de nacimiento"
              name="nacimiento"
              id="nacimiento"
              value="<%=values.nacimiento%>"
            />
            <% if (typeof errors !== "undefined") { %> <% const nacimientoError
            = errors.find(error => error.path === 'nacimiento'); %>
            <div class="invalid-feedback">
              <%= nacimientoError ? nacimientoError.msg : '' %>
            </div>
            <% } %>
          </div>
        </div>
        <div class="container-data">
          <label for="genero">Género</label>
          <select
            name="genero"
            id="genero"
            class="form-control <%= errors && errors.find(error => error.path === 'genero') ? 'is-invalid' : '' %>"
          >
          <option value="" disabled selected>Selecciona una opción</option>
            <option value="Masculino">Masculino</option>
            <option value="Femenino">Femenino</option>
            <option value="Indefinido">Indefinido</option>
            <option value="Otro">Otro</option>
          </select>
          <% if (typeof errors !== "undefined") { %> <% const generoError =
          errors.find(error => error.path === 'genero'); %>
          <div class="invalid-feedback">
            <%= generoError ? generoError.msg : '' %>
          </div>
          <% } %>
        </div>
        <div class="container-input">
          <div class="container-info">
            <label for="caracteristica">Código:</label>
            <input
              type="text"
              class="form-control <%= errors && errors.find(error => error.path === 'caracteristica') ? 'is-invalid' : '' %>"
              placeholder="Código"
              name="caracteristica"
              id="caracteristica"
              value="<%=values.caracteristica%>"
            />
            <% if (typeof errors !== "undefined") { %> 
              <% const caracteristicaError = errors.find(error => error.path === 'caracteristica'); %>
              <div class="invalid-feedback">
                <%= caracteristicaError ? caracteristicaError.msg : '' %>
              </div>
              <% } %>
          </div>
          <div class="container-info">
            <label for="telefono">Teléfono:</label>
            <input
              type="number"
              class="form-control <%= errors && errors.find(error => error.path === 'telefono') ? 'is-invalid' : '' %>"
              placeholder="Teléfono"
              name="telefono"
              id="telefono"
              value="<%=values.telefono%>"
            />
            <% if (typeof errors !== "undefined") { %> 
              <% const telefonoError = errors.find(error => error.path === 'telefono'); %>
              <div class="invalid-feedback">
                <%= telefonoError ? telefonoError.msg : '' %>
              </div>
              <% } %>
          </div>
        </div>
        <div class="container-input">
          <div class="container-info">
            <label for="pais">País</label>
            <input
              type="text"
              class=" form-control  <%= errors && errors.find(error => error.path === 'pais') ? 'is-invalid' : '' %>"
              placeholder="País"
              name="pais"
              id="pais"
              value="<%=values.pais%>"
            />
            <% if (typeof errors !== "undefined") { %>
              <% const paisError = errors.find(error => error.path === 'pais'); %>
              <div class="invalid-feedback">
                 <%= paisError ? paisError.msg : '' %>
              </div>
          <% } %>
          </div>
          <div class="container-info">
            <label for="provincia">Provincia:</label>
            <input
              type="text"
              placeholder="Provincia"
              id="provincia"
              name="provincia"
              class=" form-control  <%= errors && errors.find(error => error.path === 'provincia') ? 'is-invalid' : '' %>"
              value="<%=values.provincia%>"
            />
            <% if (typeof errors !== "undefined") { %>
              <% const provinciaError = errors.find(error => error.path === 'provincia'); %>
              <div class="invalid-feedback">
                 <%= provinciaError ? provinciaError.msg : '' %>
              </div>
          <% } %>
          </div>
        </div>
        <div class="container-data">
          <label for="civil">Estado civil</label>
          <input
            type="text"
            placeholder="Estado civil"
            name="civil"
            id="civil"
            class=" form-control  <%= errors && errors.find(error => error.path === 'civil') ? 'is-invalid' : '' %>"
            value="<%=values.civil%>"
          />
          <% if (typeof errors !== "undefined") { %>
            <% const civilError = errors.find(error => error.path === 'civil'); %>
            <div class="invalid-feedback">
               <%= civilError ? civilError.msg : '' %>
            </div>
        <% } %>
        </div>
        <div class="container-data">
<label for="cursos">Cursos y capacitaciones</label>
<textarea placeholder="Cursos y capacitaciones" name="cursos" id="" cols="30" rows="10"  class=" form-control  <%= errors && errors.find(error => error.path === 'cursos') ? 'is-invalid' : '' %>"><%=values.cursos%></textarea>
<% if (typeof errors !== "undefined") { %>
  <% const cursosError = errors.find(error => error.path === 'cursos'); %>
  <div class="invalid-feedback">
     <%= cursosError ? cursosError.msg : '' %>
  </div>
<% } %>
        </div>
        <div class="container-data">
          <label for="estudios">Nivel de estudios alcanzados:</label>
          <select name="estudios" id="estudios" class=" form-control  <%= errors && errors.find(error => error.path === 'estudios') ? 'is-invalid' : '' %>">
            <option value="" disabled selected>Selecciona una opción</option>
            <option value="curso">Curso/Bootcamp</option>
            <option value="secundario">Secundario</option>
            <option value="terciario">Terciario</option>
            <option value="universitario">Universitario</option>
            <option value="postgrado">Postgrado</option>
            <option value="otro">Otro</option>
          </select>
          <% if (typeof errors !== "undefined") { %>
            <% const estudiosError = errors.find(error => error.path === 'estudios'); %>
            <div class="invalid-feedback">
               <%= estudiosError ? estudiosError.msg : '' %>
            </div>
        <% } %>
        </div>
        <div class="container-input">
          <div class="container-info">
            <label for="estado">Estado del nivel académico:</label>
            <select name="estado" id="" class=" form-control  <%= errors && errors.find(error => error.path === 'estado') ? 'is-invalid' : '' %>">
              <option value="" disabled selected>Selecciona una opción</option>
              <option value="En curso">En curso</option>
              <option value="Graduado">Graduado</option>
              <option value="Abandonado">Abandonado</option>
            </select>
            <% if (typeof errors !== "undefined") { %>
              <% const estadoError = errors.find(error => error.path === 'estado'); %>
              <div class="invalid-feedback">
                 <%= estadoError ? estadoError.msg : '' %>
              </div>
          <% } %>
          </div>
          <div class="container-info">
            <label for="titulo">Título:</label>
            <input
              type="text"
              placeholder="Título"
              name="titulo"
              id="titulo"
              value="<%=values.titulo%>"
              class=" form-control  <%= errors && errors.find(error => error.path === 'titulo') ? 'is-invalid' : '' %>"
            />
            <% if (typeof errors !== "undefined") { %>
              <% const tituloError = errors.find(error => error.path === 'titulo'); %>
              <div class="invalid-feedback">
                 <%= tituloError ? tituloError.msg : '' %>
              </div>
          <% } %>
          </div>
        </div>
        <div class="container-data">
          <label for="estudios2">Segunda formación academica:</label>
          <select name="estudios2" id="estudios2" class="form-control">
            <option value="" disabled selected>Selecciona una opción</option>
            <option value="curso">Curso/Bootcamp</option>
            <option value="secundario">Secundario</option>
            <option value="terciario">Terciario</option>
            <option value="universitario">Universitario</option>
            <option value="postgrado">Postgrado</option>
            <option value="otro">Otro</option>
          </select>
        </div>
        <div class="container-input">
          <div class="container-info">
            <label for="estado">Estado de nivel académico:</label>
            <select name="estado2" id="" class=" form-control">
              <option value="" disabled selected>Selecciona una opción</option>
              <option value="En curso">En curso</option>
              <option value="Graduado">Graduado</option>
              <option value="Abandonado">Abandonado</option>
            </select>
          </div>
          <div class="container-info">
            <label for="titulo">Título:</label>
            <input
              type="text"
              placeholder="Título"
              name="titulo2"
              id="titulo2"
              value="<%=values.titulo2%>"
              class=" form-control"
            />
          </div>
        </div>
        <div class="container-data">
          <label for="habilidades">Habilidades:</label>
          <textarea
            placeholder="Habilidades"
            name="habilidades"
            id="habilidades"
            cols="20"
            rows="5"
          >
<%=values.habilidades%></textarea
          >
        </div>
        <div class="container-input">
          <div class="container-info">
            <label for="secundario">Título Secundario:</label>
            <input
              type="text"
              placeholder="Título ssecundario"
              name="secundario"
              id="secundario"
              class=" form-control  <%= errors && errors.find(error => error.path === 'secundario') ? 'is-invalid' : '' %>"
              value="<%=values.secundario%>"
            />
            <% if (typeof errors !== "undefined") { %>
              <% const secundarioError = errors.find(error => error.path === 'secundario'); %>
              <div class="invalid-feedback">
                 <%= secundarioError ? secundarioError.msg : '' %>
              </div>
          <% } %>
          </div>
          <div class="container-info">
            <label for="trabajo">Primer experiencia laboral:</label>
            <input
              type="text"
              placeholder="1er Trabajo"
              name="trabajo"
              class=" form-control  <%= errors && errors.find(error => error.path === 'trabajo') ? 'is-invalid' : '' %>"
              value="<%=values.trabajo%>"
            />
            <% if (typeof errors !== "undefined") { %>
              <% const trabajoError = errors.find(error => error.path === 'trabajo'); %>
              <div class="invalid-feedback">
                 <%= trabajoError ? trabajoError.msg : '' %>
              </div>
          <% } %>
          </div>
        </div>
        <div class="container-input">
          <div class="container-info">
            <label for="empleador">Empleador:</label>
            <input
              type="text"
              placeholder="Empleador"
              name="empleador"
              id="empleador"
              class=" form-control  <%= errors && errors.find(error => error.path === 'empleador') ? 'is-invalid' : '' %>"
              value="<%=values.empleador%>"
            />
            <% if (typeof errors !== "undefined") { %>
              <% const empleadorError = errors.find(error => error.path === 'empleador'); %>
              <div class="invalid-feedback">
                 <%= empleadorError ? empleadorError.msg : '' %>
              </div>
          <% } %>
          </div>
          <div class="container-info">
            <label for="posicion"> Posicion laboral:</label>
            <select name="posicion" id="" class=" form-control  <%= errors && errors.find(error => error.path === 'posicion') ? 'is-invalid' : '' %>">
              <option value="" disabled selected> Seleccione una opción</option>
              <option value="Trainne">trainne</option>
              <option value="Junior">Junior</option>
              <option value="Semi-senior">Semi-senior</option>
              <option value="Senior">Senior</option>
            </select>
            <% if (typeof errors !== "undefined") { %>
              <% const posicionError = errors.find(error => error.path === 'posicion'); %>
              <div class="invalid-feedback">
                 <%= posicionError ? posicionError.msg : '' %>
              </div>
          <% } %>
          </div>
        </div>
        <div class="container-data">
          <label for="descripcionLaboral">Descripción del puesto:</label>
          <textarea
            placeholder="Descripcion laboral"
            name="descripcionLaboral"
            id="descripcionLaboral"
            cols="20"
            class=" form-control  <%= errors && errors.find(error => error.path === 'descripcionLaboral') ? 'is-invalid' : '' %>"
            rows="5"
          >
<%=values.descripcionLaboral%></textarea>
          <% if (typeof errors !== "undefined") { %>
            <% const descripcionLaboralError = errors.find(error => error.path === 'descripcionLaboral'); %>
            <div class="invalid-feedback">
               <%= descripcionLaboralError ? descripcionLaboralError.msg : '' %>
            </div>
        <% } %>
        </div>
        <div class="container-input">
          <div class="container-info">
            <label for="nacionalidad">Nacionalidad:</label>
            <input
              type="text"
              placeholder="Nacionalidad"
              name="nacionalidad"
              id="nacionalidad"
              class=" form-control  <%= errors && errors.find(error => error.path === 'nacionalidad') ? 'is-invalid' : '' %>"
              value="<%=values.nacionalidad%>"
            />
            <% if (typeof errors !== "undefined") { %>
              <% const nacionalidadError = errors.find(error => error.path === 'nacionalidad'); %>
              <div class="invalid-feedback">
                 <%= nacionalidadError ? nacionalidadError.msg : '' %>
              </div>
          <% } %>
          </div>
          <div class="container-info">
            <label for="puesto2">Segunda experiencia laboral:</label>
            <input
              type="text"
              placeholder=" 2do Trabajo"
              name="puesto2"
              class=" form-control"
            />
          </div>
        </div>
        <div class="container-input">
          <div class="container-info">
            <label for="empleador2">Empleador:</label>
            <input
              type="text"
              placeholder="Empleador"
              name="empleador2"
              id="empleador2"
              class=" form-control"
              value="<%=values.empleador2%>"
            />
          </div>
          <div class="container-info">
            <label for="posicion2"> Posicion laboral:</label>
            <select name="posicion2" id="" class=" form-control">
              <option value="" disabled selected> Seleccione una opción</option>
              <option value="Trainne">trainne</option>
              <option value="Junior">Junior</option>
              <option value="Semi-senior">Semi-senior</option>
              <option value="Senior">Senior</option>
            </select>
          </div>
        </div>
        <div class="container-data">
          <label for="descripcionLaboral2">Descripción del puesto:</label>
          <textarea
            placeholder="Descripcion laboral"
            name="descripcionLaboral2"
            id="descripcionLaboral2"
            cols="20"
            rows="5"
          >
<%=values.descripcionLaboral2%></textarea
          >
        </div>
        <div class="container-input">
          <div class="container-info">
            <label for="linkedin">Perfil de linkedin:</label>
            <input
              type="text"
              placeholder="Linkedin"
              name="linkedin"
              id="linkedin"
              class=" form-control  <%= errors && errors.find(error => error.path === 'linkedin') ? 'is-invalid' : '' %>"
              value="<%=values.linkedin%>"
            />
            <% if (typeof errors !== "undefined") { %>
              <% const linkedinError = errors.find(error => error.path === 'linkedin'); %>
              <div class="invalid-feedback">
                 <%= linkedinError ? linkedinError.msg : '' %>
              </div>
          <% } %>
          </div>
          <div class="container-info">
            <label for="redes">Red social de preferencia:</label>
            <input
              type="text"
              placeholder="Redes"
              name="redes"
              value="<%=values.redes%>"
            />
          </div>
        </div>
        <div class="container-input">
          <div class="container-info">
          <label for="cv">Curriculum:</label>
          <input
            type="file"
            name="cv"
            id="cv"
            class="input-file <%= errors && errors.find(error => error.path === 'cv') ? 'is-invalid' : '' %>"
            accept=".pdf"
          />
          <% if (typeof errors !== "undefined") { %>
            <% const cvError = errors.find(error => error.path === 'cv'); %>
            <div class="invalid-feedback">
               <%= cvError ? cvError.msg : '' %>
            </div>
        <% } %>
        </div>
        <div class="container-info">
          <label for="image">Foto de perfil:</label>
          <input
            type="file"
            name="image"
            id="image"
            class="input-file <%= errors && errors.find(error => error.path === 'image') ? 'is-invalid' : '' %>"
            accept="image/*"
          />
          <% if (typeof errors !== "undefined") { %>
            <% const imageError = errors.find(error => error.path === 'image'); %>
            <div class="invalid-feedback">
               <%= imageError ? imageError.msg : '' %>
            </div>
        <% } %>
        </div>
        </div>
        <div class="container-btn">
          <button class="btn btn-outline-light" type="submit">Crear</button>
        </div>
      </div>
    </form>
  </section>
</main>